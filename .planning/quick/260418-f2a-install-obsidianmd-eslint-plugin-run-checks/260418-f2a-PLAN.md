---
quick_id: 260418-f2a
slug: install-obsidianmd-eslint-plugin-run-checks
description: Install eslint-plugin-obsidianmd and run checks; add Toggl as brand name
date: 2026-04-18
must_haves:
  truths:
    - eslint-plugin-obsidianmd installed and configured
    - eslint.config.mjs uses obsidianmd recommended config
    - Toggl added to brands list in sentence-case rule
    - ESLint runs cleanly (no errors, warnings reviewed)
  artifacts:
    - eslint.config.mjs (updated)
    - package.json (updated)
    - package-lock.json (updated)
---

# Quick Task 260418-f2a: Install eslint-plugin-obsidianmd and run checks

## Tasks

### T1: Install eslint-plugin-obsidianmd and required peer deps

The plugin (`eslint-plugin-obsidianmd`) requires ESLint `>=9.0.0 <10.0.0`. Current project has
`eslint@10.2.0`. Must downgrade ESLint to v9 and install peer deps.

Files: `package.json`, `package-lock.json`

Action:
- Downgrade `eslint` to `^9.0.0`
- Install `eslint-plugin-obsidianmd` from GitHub
- Install peer deps: `@eslint/js`, `@eslint/json`, `typescript-eslint`

Verify: `npm ls eslint-plugin-obsidianmd` shows installed package

Done: package.json reflects new deps, no peer dep warnings

### T2: Update eslint.config.mjs to use obsidianmd plugin

Files: `eslint.config.mjs`

Action:
- Import `eslint-plugin-obsidianmd`
- Spread `obsidianmd.configs.recommended` at top
- Add `Toggl` to `brands` array in `ui/sentence-case` rule (product name must not be lowercased)
- Configure to check `src/**/*.ts`, `tests/**/*.ts`, `manifest.json`

Verify: `npx eslint --print-config src/main.ts` shows obsidianmd rules active

Done: eslint.config.mjs updated and valid

### T3: Run ESLint, apply auto-fixes, fix remaining violations

Files: `src/*.ts`

Action:
- Run `npx eslint src/ tests/ --fix`
- Review remaining errors/warnings
- Fix any non-auto-fixable violations in source files

Verify: `npx eslint src/ tests/` exits 0 (or only expected warnings)

Done: ESLint passes cleanly
